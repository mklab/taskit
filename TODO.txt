t: KIT48のために行ったとりあえずの実装。
*: 手をつけてない項目

v1.0 基本機能実装
-----------------
* 管理者ページ
	* ユーザー編集(登録、削除、パスワード・権限の編集)
* 提出失敗時の処理
	Receiver#onFailure()でステータスバーに表示するようにはしているが分かりにくい。
	onFailureが必ず行えたらいいが、失敗はしたがonFailureが呼ばれる前にActivityが
	切り替えられたらどうなるか未調査。

v1.1 提出状況分析(どの生徒が危ないかなど)
---------------------------------------
* 全生徒の出席・提出状況一覧表示
t 成績の集計
	サーバーサイドで計算は行っている。
	但し成績に変更があると直ちに再計算しており、若干コスト的な心配がある。
	今は計算は30msくらいだけれどもデータベースのレコード数が増えるとどうなるか分からない。
	生徒別の成績ビューでソートすることはできるが、集計結果を全体で表示はしていない。
	「全生徒の出席・提出状況一覧表示」に入れてもいいかも。

v1.2 リアルタイム性
-------------------
t Server Pushの実現。定期的な監視ではなく、リアルタイムに。特にTA Call
	サーバープッシュの接続が途切れた時の処理がまだ何も行っていない。
t 競合処理。定期的な更新なので、例えば、修正 -> 修正情報送信中 -> 定期更新 -> 修正情報送信完了
  という順で処理が行われると、自分の修正が行われていないかのように表示されてしまう。

v1.3 モバイル端末のサポート
--------------------------



その他
* アンケート
  予め授業の内容を項目に分けて用意しておき、それに対し分からない学生は投票していく。
  先生の補足説明の参考にする。
t 講義部屋マップ
  TA Callだけではどこにいる人が呼んでいるか分からないので、マップを用意して
  TA Callがされたら、マップ中の対象の席をハイライト表示。座る場所は強制しないといけないけれども・・。
  IPで判定すれば座る場所すら縛らなくなるかも。
  -> だいたい実装完了。ただし、マップを設定する方法は今は直接サーバーのアプリケーションルートディレクトリ
     に"map.csv"で配置していないといけないという縛りがある。
     また、今はヘルプコールからしか見れないので他のところでも見れるようにしていいかも。
     
     ハイライトしていないマップをクライアントが受け取り、その画像のクリック(タップ)した座標を
     サーバーに送り、座標からサーバー上でどの生徒が選択されたかを判定、クライアントに返し、
     クライアントではその学生のページに移動。というのもやりたい。
     
     画像は毎回生成しているのでサーバー側でキャッシュしたい(CachedRoomMapFactoryとか作ってデコレーター的に)。
* ツールバーのツールチップ表示
  スマートフォンとかでツールチップはどう実装する？長押しはたぶんメニューが表示されてしまう。
